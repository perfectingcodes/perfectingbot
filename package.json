{
  "name": "perfectingbot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "setup": "node -e \"const f=require('fs');if(f.existsSync('.env')){console.log('.env already exists \u2014 leaving it alone');}else{f.copyFileSync('.env.example','.env');console.log('created .env \u2014 add FOMO_API_KEY and SOLANA_RPC_URL, then: npm run doctor');}\"",
    "dev": "node --env-file-if-exists=.env --experimental-strip-types src/main.ts",
    "scanner": "node --env-file-if-exists=.env --experimental-strip-types src/index.ts",
    "serve": "node --experimental-strip-types src/serve.ts",
    "replay": "node --experimental-strip-types src/replay.ts",
    "doctor": "node --env-file-if-exists=.env --experimental-strip-types src/doctor.ts",
    "build": "tsc",
    "start": "node dist/main.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "viem": "^2.21.0",
    "ws": "^8.21.3"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "@types/ws": "^8.18.1",
    "typescript": "^5.6.0"
  }
}
